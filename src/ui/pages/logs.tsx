/**
 * 日志页面
 *
 * 职责:
 *   - 提供向后兼容的日志页面导出
 *   - 标记日志功能已迁移
 *
 * 模块功能:
 *   - 空实现(功能已迁移到文件存储)
 *
 * 使用场景:
 *   - 旧代码引用时保持兼容
 *   - 日志功能已改为文件存储
 *
 * 边界:
 *   1. 已废弃，仅保留向后兼容
 *   2. 日志文件存放于 ~/crab/logs/
 *   3. 使用 Ctrl+L 打开日志目录
 *
 * 流程:
 *   1. 空实现，不执行任何操作
 */
/** 已废弃，不应在新代码中使用:日志不再通过 TUI 查看 */
export function LogsPage() {
  // 已废弃，不应在新代码中使用:日志不再通过 TUI 查看
  // 请使用 Ctrl+L 打开 ~/crab/logs/ 目录
}
