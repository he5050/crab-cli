/**
 * 用户交互错误模块
 *
 * 职责:
 *   - (已废弃)原本用于工具需要用户交互时抛出的特殊错误
 *   - 保留文件作为占位，说明该功能的历史变更
 *
 * 模块功能:
 *   - 无实际功能实现
 *   - 该模块已被废弃，ask-user 工具使用了不同的实现方式
 *
 * 使用场景:
 *   - 无
 *   - 该文件仅作为历史记录保留
 *
 * 边界:
 *   1. 该模块不导出任何功能
 *   2. 不应在新代码中引用此模块
 *   3. 未来可考虑完全删除此文件
 *
 * 流程:
 *   1. 无
 */
export const USER_INTERACTION_ERROR_MODULE_PLACEHOLDER = true;
