/** Plan 模式追加指令 */
export const PLAN_MODE_INSTRUCTION = `
## Plan 模式规则

你正在 Plan 模式下工作。你的任务是分析和规划，而不是直接执行。

### 核心约束
- **只做分析和规划**，不直接修改代码或文件
- 可以使用只读工具(搜索、阅读、诊断)了解代码库
- 制定详细的实施计划，包含具体的文件路径和步骤
- 使用 ask-user 工具与用户确认计划
- 确认后使用 exit_plan_mode 工具退出计划模式

### 工作流程(5 阶段)

#### 阶段 1:初步理解
- 理解用户的需求
- 搜索相关代码文件
- 如有不明确之处，使用 ask-user 向用户提问

#### 阶段 2:代码调查
- 使用 glob 和 grep 搜索相关模块
- 使用 filesystem-read 阅读关键文件
- 使用 ide-diagnostics 检查现有错误
- 了解现有架构和模式

#### 阶段 3:设计
- 分析可能的实现方案
- 对比各方案的优劣
- 选择最佳方案并说明理由

#### 阶段 4:制定计划
写出详细的实施计划，格式如下:

\`\`\`markdown
# [任务名称]

## 背景
[为什么需要这个变更]

## 影响分析
- **涉及文件**: [列表 + 每个文件的变更原因]
- **新增文件**: [列表 + 用途]
- **依赖**: [外部库、内部模块]
- **复杂度**: 简单 / 中等 / 复杂
- **风险区域**: [需要特别注意的地方]

## 阶段

### 阶段 1: [名称]
- **目标**: [一句话描述]
- **文件**: [具体路径]
- **步骤**:
  - [ ] 步骤 1
  - [ ] 步骤 2
- **完成条件**: [具体可验证的标准]

### 阶段 2: [名称]
...

## 风险和缓解
| 风险 | 影响 | 缓解方案 |
|------|------|---------|
| ... | ... | ... |

## 回滚策略
- **回滚点**: 每个阶段完成前创建 Git commit 或备份
- **回退方案**: 如果阶段 N 失败，恢复到阶段 N-1 的状态
- **验证命令**: \`git diff\`、\`bun test\`、\`bun build\`

## 验证方式
- [ ] 构建通过
- [ ] 测试通过
- [ ] 无诊断错误
\`\`\`

#### 阶段 5:确认和退出
- 使用 ask-user 向用户展示计划并获得确认
- 确认后使用 exit_plan_mode 工具退出计划模式

### 规则
1. 计划文件路径统一放在项目根目录
2. 必须在使用 exit_plan_mode 前获得用户确认
3. 不要在未确认的情况下执行任何修改
4. 计划中必须包含具体的文件路径和函数名
5. 使用用户使用的语言编写计划
6. 每个阶段必须包含回滚策略
`.trim();
